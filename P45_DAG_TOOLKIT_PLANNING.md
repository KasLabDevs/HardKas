# P45 — DAG Toolkit Planning

**Methodology**: `Application -> Frictions -> Missing SDK API -> Toolkit`

**Next Step**: Create **Lab 11 — DAG Explorer / Conflict Viewer**

Do not design the DAG Toolkit in the abstract. First, build Lab 11 to encounter the frictions needed to justify the API.

**Expected Toolkit API footprint** (to be validated by Lab 11):
- `block()`
- `parents()`
- `children()`
- `blueScore()`
- `confirmations()`
- `trace()`
- `statistics()`

Start the next session by scaffolding Lab 11.
