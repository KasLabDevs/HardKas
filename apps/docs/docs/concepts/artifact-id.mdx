---
id: artifact-id
title: Artifact Identity
---

import QualificationContext from '@site/src/components/QualificationContext';

# Artifact Identity

<QualificationContext capabilityId="artifact-identity" />

This page demonstrates the usage of a centralized qualification data model injected via MDX.
The component above renders state dynamically derived from `docs-data/qualification.ts`.

If the evidence level or environment support changes, we simply update the central dataset, and this page will automatically reflect the truth.
