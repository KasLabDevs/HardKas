# Framework Frictions Discovered

During the implementation of App 3 (Explorer Backend), no framework frictions were discovered. The public API of `@hardkas/toolkit` (specifically `IndexerToolkit` and `DagToolkit`) was fully sufficient for building a high-concurrency DAG explorer backend with a REST API.
