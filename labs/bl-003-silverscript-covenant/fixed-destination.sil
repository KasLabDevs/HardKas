pragma silverscript ^0.1.0;

contract FixedDestination() {
    entry spend() {
        byte[] expectedSpk = byte[](0xaa00);
        require(tx.outputs.length == 1);
        require(tx.outputs[0].scriptPubKey == expectedSpk);
    }
}